# Folio+ — Notes pour Claude Code

Notes condensées à l'essentiel (réduit le 13/07 — l'historique détaillé par session existe encore dans `git log`/les commits si besoin de retrouver le raisonnement complet d'un choix).

## Design system

- **`DESIGN.md`** (racine du repo) est la référence **unique** pour les couleurs, la nomenclature M3 et les ratios WCAG déjà vérifiés. Le consulter avant tout changement de `src/styles.css` ou de token. Règle permanente : toute session qui touche le design system met à jour `DESIGN.md` dans la foulée, pas seulement sur demande.
- Tokens définis **uniquement** dans `src/styles.css` (`@theme inline` + `:root`/`.dark`/`:root:not(.dark)`). Pas de `tailwind.config.*` (Tailwind v4, CSS-first).
- `src/components/ui/` (shadcn) = code mort confirmé, ne pas modifier sans demande explicite de le réactiver.
- **Icônes** : Lucide uniquement (`lucide-react`), jamais Material Symbols. Règle globale `styles.css` (`@layer base`) : `.lucide { stroke-width: 1.5 }` (uniforme, plus d'exception sous 16px) + `cursor: pointer` systématique sur `button`/`[role="button"]`/`a[href]`/`select`/`label[for]`/checkbox/radio. **Exception explicite (14/07)** : `react-icons/fa6` (`FaLinkedin`) pour le logo de marque LinkedIn du profil public — Lucide n'a pas de vrai logo LinkedIn. Ne pas généraliser au-delà de ce cas. Le bouton "X" (`FaXTwitter`) a été retiré le 09/09 (remplacé par un bouton "e-mail", icône Lucide `AtSign`) — le bouton du profil public copie l'e-mail dans le presse-papiers au clic (`navigator.clipboard.writeText`, même pattern que le bouton "copier l'URL publique" de `ParametresTab` — icône `AtSign`→`Check` 1,5s, `aria-label` "Copier l'e-mail"→"E-mail copié"). **`email` devenu un champ DB réel le même jour** (`designer_profiles.email`, remplace `twitter_url` **supprimée** — plus aucun usage nulle part une fois le bouton "X" retiré) : le champ admin "X (Twitter)" (`ParametresTab`) est remplacé par "E-mail" (`s-email`, `type="email"`, validation dédiée `isValidEmail` dans `src/lib/utils.ts`, même convention "vide = optionnel, valide" que `isValidUrl`). Le `<form>` de `ParametresTab` a gagné `noValidate` à cette occasion : sans lui, la validation HTML5 native de `type="email"` interceptait la soumission avant `handleSave`, empêchant le message d'erreur custom (`fieldError`) de jamais s'afficher — même pattern déjà en place sur `ContactForm.tsx`. Le bouton "copier l'e-mail" du Hero lit désormais cette valeur DB (avec repli sur le mock si `null`), plus une constante figée — vérifié en direct en changeant temporairement la valeur en base et en confirmant que le bouton copie la nouvelle valeur.
- **Nav admin** (`AdminSidebar`, `AdminPage.tsx`) : couleur par section via `NAV_ACTIVE_CLASSES`, détail complet (valeurs, ratios) dans `DESIGN.md` section "Couleurs de nav active + badges". Les halos `SectionAurora` de chaque section sont volontairement dissociés de cette couleur de nav depuis le 13/07 — ne pas chercher à les réaligner (exception : Paramètres, dont le nav + halo ont été réalignés sur `teal` le 14/07 pour matcher Vue d'ensemble, sur demande explicite). Les cartes "accès rapide" du Dashboard (`QuickAccessCard`) reprennent elles aussi la couleur nav de leur onglet cible, pas une teinte partagée — depuis le 17/07, `QuickAccessCard` a deux dispositions distinctes (`md:hidden`/`hidden md:flex`, mobile vs desktop, cf. `DESIGN.md` section "Navigation mobile"), toucher une seule des deux sans vérifier l'autre est une régression facile. `AdminSidebar` elle-même est cachée sur mobile (`hidden md:flex`), remplacée par `AdminMobileBottomNav`.
- **Filtres admin** : `AdminFilterBar` (`src/components/AdminFilterBar.tsx`) est le composant de référence pour tout filtre côté dashboard admin (Projets/Demandes/Contacts/Veille + les listes Vue d'ensemble) — reprend le shell visuel du bouton "Filtrer" de `FilterBar.tsx` (catalogue public) mais généralisé à des groupes arbitraires. Ne pas recréer de pills ad hoc (`veillePillCls` encore présent uniquement pour le filtre mois de la mini-table Veille du Dashboard, cas non couvert par `AdminFilterBar`).
- **Tooltips** : `TooltipProvider` (Radix, `src/components/ui/tooltip.tsx`) actif globalement depuis `RootLayout.tsx` (14/07 — auparavant quasi inutilisé). Tout bouton icône-seul (sans texte visible) doit être enveloppé dans `IconTooltip` (`src/components/IconTooltip.tsx`, reprend le libellé `aria-label` existant).
- **⚠️ Divergence non résolue** : `secondary` vaut `#7c3aed` dans `styles.css` (dark) mais `DESIGN.md` liste `#D2BBFF` — à trancher avec l'utilisatrice avant la passe light mode, ne pas corriger à l'aveugle.
- **Reste à faire — mode light** : les 8 tokens de marque (`primary`/`secondary`/`tertiary` + variantes container/on-container) n'ont aucune valeur en light, héritent silencieusement du dark. Tâche reportée explicitement par l'utilisatrice. Prévoir aussi le remplacement des bordures `border-white/X` (quasi invisibles en light).
- **⚠️ `overflow-x: hidden` sur `body` casse silencieusement tout `position: sticky` descendant** : repéré le 02/09 sur l'aside de `ProjectDetailPage.tsx` (ne restait jamais accroché au scroll, aucune erreur console). Cause : le fixup du spec CSS overflow — dès qu'`overflow-x` est non-`visible` sur un élément, `overflow-y` calcule automatiquement à `auto` sur ce même élément, même sans déclaration explicite. `styles.css` déclarait `overflow-x: hidden` sur `html, body` (filet anti-débordement horizontal du 22/07) : `body` devenait donc un second conteneur de scroll aux yeux de `sticky` (dont le *containing block* se résout à la plus proche ancêtre avec un `overflow` calculé non-`visible`, peu importe qu'elle scrolle réellement) — et comme `body` ne déborde jamais de son propre contenu, `sticky` s'y comportait comme `static`. Fix : `overflow-x: hidden` déplacé sur `html` seul (déjà la racine réelle du scroll du document, protège tout aussi bien les descendants via `body`). **Diagnostic qui a fonctionné : scroller réellement et lire `element.getBoundingClientRect().top` à plusieurs paliers de scroll** — `getComputedStyle(el).position === 'sticky'` seul ne suffit pas à confirmer que le sticky fonctionne, il confirme juste que la propriété CSS est bien appliquée, pas son effet réel.

## Base de données / RLS — pièges à ne pas reproduire

- **`security_invoker` sur les vues** : `CREATE OR REPLACE VIEW` ne préserve pas cette option si elle n'est pas re-précisée — retombe en mode `SECURITY DEFINER` implicite (bypass RLS). Toujours `ALTER VIEW ... SET (security_invoker = true)` après un `CREATE OR REPLACE VIEW`, et vérifier avec `select reloptions from pg_class where relname = '...'` (`null` = dangereux). Déjà arrivé une fois sur `projects_catalog_view` (draft visible par anon).
- **Ne jamais supposer qu'un `UPDATE`/`DELETE` a réussi juste parce qu'il n'a pas levé d'erreur** — RLS peut filtrer 0 ligne silencieusement. Toujours vérifier via `RETURNING ... INTO` + check `null`/`GET DIAGNOSTICS` avant de retourner un succès à l'appelant (pattern déjà en place dans `soft_delete_project`, `updateProject*`, `approve/rejectAccessRequest`, `syncProjectTags` — via comparaison du nombre de lignes insérées aux IDs attendus —, et `AccessRequestModal.handleSubmit`/`submitPendingAccessRequest` pour l'`UPDATE user_profiles`).
- **Récursion croisée RLS** : deux policies sur deux tables qui se référencent mutuellement en sous-requête directe déclenchent `42P17`. Passer par des fonctions `SECURITY DEFINER` (comme `get_my_role()`) qui bypassent RLS plutôt qu'une sous-requête brute inter-tables.
- **`REVOKE EXECUTE FROM PUBLIC` ne suffit pas toujours** : les fonctions du schéma `public` peuvent aussi recevoir un `GRANT` explicite à `anon`/`authenticated` (privilège par défaut Supabase sur les nouvelles fonctions), distinct de l'héritage via `PUBLIC`. Un `REVOKE ... FROM anon, authenticated` qui ne cible pas aussi `PUBLIC` (et vice versa) peut ne rien changer en pratique — vérifier avec `has_function_privilege('anon', oid, 'EXECUTE')` après coup, pas seulement relire la migration. Repéré le 15/07 sur `handle_new_user`/`prevent_role_self_update` (hérité de PUBLIC) vs `dispatch_webhook`/`create_access_request_notifications` (GRANT explicite) qui nécessitaient chacun la variante différente.
- **Buckets storage publics (`public: true`) n'ont pas besoin de policy SELECT** sur `storage.objects` : la lecture par URL directe ne passe pas par RLS. Une policy SELECT large (`using (bucket_id = '...')`) autorise en plus le *listing* de tous les fichiers du bucket, inutile et à éviter (advisor Supabase "Public Bucket Allows Listing") — retiré de `project-thumbnails`/`designer-photos` le 15/07.
- **⚠️ Mais aucune policy SELECT du tout casse silencieusement DELETE/UPDATE authentifiés sur ce même bucket** — repéré le 28/09 en vérifiant en direct (appel `DELETE` authentifié, pas juste `tsc`/lecture de code) le flux de suppression d'une image de galerie (`project-gallery`, nouveau bucket) : l'API Storage a besoin d'une requête interne, elle-même soumise à RLS, pour retrouver l'objet avant de le modifier/supprimer. Sans **aucune** policy SELECT (même admin-only), cette recherche échoue et l'opération est bloquée — mais `supabase.storage.from(...).remove([path])` ne renvoie alors **aucune `error`**, juste un tableau vide/partiel (même piège que le pattern RLS-silencieuse déjà documenté ci-dessus pour Postgres, mais côté Storage). Fix : une policy SELECT `to authenticated using (bucket_id = '...' and get_my_role() = 'admin')` (jamais `anon`, pour ne pas réintroduire le problème de listing public) — testé en direct, passe de 403 à 200. Toujours vérifier la `data` retournée par `.remove()` en plus de `error` (cf. `deleteProjectGalleryImage`, `src/lib/storage.ts`). **`project-thumbnails`/`designer-photos` avaient le même trou depuis le 15/07** (leur policy SELECT avait été retirée sans policy admin-only de repli) — confirmé et corrigé le 03/09 (`project_thumbnails_select_admin`/`designer_photos_select_admin`, migration `20260903184843_project_thumbnails_designer_photos_select_admin_for_mutations.sql`, même repro en direct : 403/`data: []` sans la policy → 200/fichier réellement supprimé avec). `deleteProjectThumbnail` (`src/lib/storage.ts`) vérifie désormais `data` en plus de `error`, même pattern que `deleteProjectGalleryImage`. 6 fichiers orphelins trouvés en base et nettoyés à cette occasion (4 dans `project-thumbnails`, 2 dans `designer-photos`) — un seul projet-thumbnail orphelin (`365c6f35.../1783801621557.png`) provenait en fait d'un autre trou, non lié à ce bug : **aucun flux ne supprime l'ancien fichier lors du remplacement d'une miniature/photo pendant une édition** (`deleteProjectThumbnail` n'est appelée que depuis `softDeleteProject`, jamais lors d'un remplacement ; `uploadDesignerPhoto` n'a même pas de fonction de suppression associée) — pas corrigé dans cette session, nouvelle piste si le nettoyage régulier des buckets doit être automatisé.
- **Bucket `designer-cv` (07/09, chantier « Parcours »)** : créé avec les 4 policies (insert/update/delete/select, toutes admin-only) dès la migration initiale, pas en 2 temps — applique directement la leçon ci-dessus au lieu de la redécouvrir. Vérifié en direct via des appels REST Storage authentifiés (upload admin → 200 ; delete anon → 200 mais `data: []`, aucune ligne réellement supprimée puisque anon ne peut pas retrouver l'objet via la policy SELECT admin-only ; delete admin → 200 avec `data.length === 1`) et un insert anon (403, `row-level security policy`) — les deux sens (insert explicite refusé, delete silencieusement no-op) confirmés avant de considérer le bucket sûr.
- **Une policy `INSERT` doit valider TOUTES les colonnes sensibles, pas juste l'`id`** : `user_profiles_insert_trigger_only` ne vérifiait que `id = auth.uid()`, jamais `role` — un utilisateur authentifié aurait pu s'auto-attribuer `role = 'admin'` via un INSERT direct si sa ligne venait à manquer (ex. après un `DELETE` admin, policy `user_profiles_delete_admin`). Corrigé le 15/07 (`AND role = 'pending'` ajouté au `WITH CHECK`). Le nom d'une policy ("trigger_only") n'a aucune valeur de sécurité en soi — seul le `WITH CHECK` compte.
- **`user_profiles_update_own`** (`WITH CHECK (id = auth.uid())`) ne restreint pas non plus la colonne `role` — à elle seule elle laisserait n'importe qui s'auto-promouvoir admin via `UPDATE`. Ce qui bloque réellement ça aujourd'hui est le trigger `trg_prevent_role_self_update` (`BEFORE UPDATE OF role`, fonction `prevent_role_self_update()`) — point de défaillance unique repéré le 15/07 (`RAPPORT_SECURITE.md`) : ne jamais supprimer/modifier ce trigger sans réaliser qu'il est la seule protection sur `role`, la RLS seule ne suffit pas.
- **Vérifier qu'un secret n'a jamais fuité exige de scanner tout l'historique git (`git log --all -p`), pas seulement l'état courant des fichiers.** Repéré le 15/07 : `CRON_SYNC_SECRET` avait été codé en dur dans `sync-notion-veille/index.ts` au commit `d450e43` (13/07), puis déplacé vers `Deno.env.get()` au commit suivant `c43fbea` — mais la valeur elle-même n'avait jamais changé, et ce commit était déjà poussé sur `origin/main`. Un `grep` sur l'état courant du repo ne l'aurait jamais trouvé. Rotation effectuée le 15/07 (nouvelle valeur en Vault via `vault.update_secret`) — la mise à jour du secret Edge Function correspondant (Dashboard ou `supabase secrets set`) reste une étape manuelle, aucun outil MCP disponible pour ça.
- **Aucun trigger générique `updated_at` n'existe côté `public`** (seul `storage.update_updated_at_column()` existe, interne au schéma `storage`) — vérifié le 07/09 en créant `experiences` : `projects`/`designer_profiles`/etc. ne maintiennent pas non plus `updated_at` automatiquement aujourd'hui (colonne `default now()` à l'insert, jamais rafraîchie à l'update). Ne pas supposer qu'un tel trigger existe déjà pour une nouvelle table sans vérifier (`select * from information_schema.triggers where trigger_name ilike '%updated_at%'`) — `experiences.updated_at` a la même limite que le reste du schéma, cohérent avec l'existant plutôt qu'un correctif isolé.
- **Une sous-requête corrélée dans un `WITH CHECK` ne peut pas référencer la ligne en cours d'insertion en réutilisant un nom de colonne nu si la sous-requête scanne la même table** : la portée SQL résout la référence vers l'alias le plus proche (celui de la sous-requête), pas vers la nouvelle ligne — silencieusement toujours vrai, pas d'erreur. Repéré le 16/07 sur `contacts_insert_anyone` (1ère version de la limite de fréquence, `c.email = email` devenait `c.email = c.email`) : la policy semblait correcte à la lecture, mais 4 soumissions rapides du même email passaient toutes. Contournement : extraire le calcul dans une fonction `SECURITY DEFINER` qui prend la valeur en paramètre (`contacts_recent_count(email)`) — `SECURITY DEFINER` nécessaire ici aussi car la policy SELECT sous-jacente est admin-only, donc un simple appel en `SECURITY INVOKER` ne verrait aucune ligne pour `anon`/`authenticated` et compterait toujours 0.

## Flux métier

- **Accès confidentiel (F-12)** : 4 états carte (`none`/`pending`/`refused`/`granted`, calculés dans `CataloguePage.resolveAccess`). Un accès `approved` débloque **un seul projet**, jamais le rôle global (seul `validated_visitor` débloque tout). **F-11 (auto-approbation) supprimée** (13/07) — toute demande passe désormais systématiquement par `pending` puis validation manuelle de Léa, quel que soit `sensitivity_level`. Si "Confirm email" est activé côté Auth, `AccessRequestModal.handleSubmit` sauvegarde la sélection de projets/message en `localStorage` (`src/data/accessRequests.ts`, `savePendingAccessRequest`/`takePendingAccessRequest`) avant le `signUp()` — sinon perdue tant que `data.session` n'est pas immédiat. Reprise automatique via l'écoute globale `SIGNED_IN` dans `RootLayout.tsx` (`submitPendingAccessRequest`), montée une seule fois. **Course entre `handleSubmit` et cette reprise `SIGNED_IN`** (17/07) : quand "Confirm email" est désactivé, `signUp()` renvoie une session immédiate et déclenche `SIGNED_IN` — les deux chemins tentaient alors d'insérer la même ligne `access_requests` (409 réseau visible en console, toléré via le code Postgres `23505`). `handleSubmit` utilise désormais `takePendingAccessRequest()` (lecture + suppression atomique, déjà utilisé côté `RootLayout`) au lieu de construire ses propres lignes indépendamment : le premier des deux chemins à s'exécuter traite la demande, l'autre reçoit `null` et ne fait rien — plus de double `INSERT` possible, tolérance `23505` retirée des deux côtés (devenue inutile).
- **RGPD (F-24)** : self-service uniquement (`anonymize-rgpd`, cible toujours le JWT appelant). Anonymise (`user_profiles`/`access_requests.message`/`contacts`), ne supprime jamais la ligne `user_profiles` (FK réelles depuis `access_requests`/`contacts`), supprime `auth.users` en dernier.
- **Paramètres admin (`ParametresTab`)** : branché au réel (14/07) — `src/data/designer.ts` (`getDesignerProfile`/`updateDesignerProfile`), écrit sur `designer_profiles` (colonnes `profession`/`adjective` ajoutées cette session, absentes jusque-là) et `admin_settings` (upsert, table vide avant le premier `cal_username`). Toggle "Modifier mes informations" : champs en lecture seule tant que non cliqué. `firstName`/`lastName`/`email`/`location` restent statiques (`src/data/designer.ts`, mock), aucune colonne DB, absents du formulaire (brièvement réintégrés en lecture seule le 15/07 puis retirés à nouveau le même jour — pas de valeur ajoutée en V1). Lecture publique de `cal_username` (pour afficher/masquer le widget Cal.com sur `/[slug]`) via `get_public_cal_username()` `SECURITY DEFINER` — `admin_settings` n'a qu'une policy SELECT admin-only, un visiteur anonyme ne peut pas la lire directement.
- **Expériences (07-08/09, schéma retouché deux fois le 08/09)** : table `experiences` (FK `designer_profile_id → designer_profiles(id)`, pas `user_profiles` — cohérent avec `cv_url`/`experiences_intro` ajoutées sur `designer_profiles`), RLS lecture publique filtrant `deleted_at is null` + policy admin `FOR ALL`. Soft delete uniquement (pas de restauration dans ce périmètre). `project_url` (texte libre) remplacé par `project_id` (FK nullable → `projects.id`, sélectionné dans un `<select>` admin plutôt que saisi). `short_desc` ajoutée puis, dans une 2e migration le même jour, **fusionnée avec `context`** (colonne `context` supprimée) — un seul champ affiché dans l'en-tête de l'accordéon désormais, contenu retravaillé à partir du CV le plus récent de l'utilisatrice à cette occasion (title/end_date de la 1ère expérience corrigés au passage : "Product Designer Consultante", fin août 2026, n'était plus un poste en cours). **`bullets` conservée** (affichée dans le contenu déplié, juste avant le bloc Impact). `src/data/experiences.ts` (mirroir `src/data/projects.ts`) : `getExperiences()` (public, `.is("deleted_at", null)` explicite en plus de la RLS) / `getExperiencesAdmin()` (toutes les lignes) / `createExperience`/`updateExperience`/`updateExperienceOrder`/`softDeleteExperience` (pattern RETURNING partout). CRUD admin dans `ParametresTab` (`ExperiencesManager`) : réordonnancement à persistance immédiate (2 appels `updateExperienceOrder` par déplacement, refetch complet en cas d'échec partiel plutôt qu'un rollback optimiste qui serait faux si un seul des deux a réussi).
- **Bouton d'accès confidentiel par expérience (08/09)** : le bouton générique "Accéder aux projets confidentiels" du Hero (`ProfilePage.tsx`, ouvrait `AccessRequestModal` sans présélection) a été **retiré**, remplacé par un bouton scopé par item d'accordéon lié à un projet confidentiel (`AccessRequestModal` avec `initialProject` pré-rempli). La logique F-12 (`resolveAccess`, 4 états `none`/`pending`/`refused`/`granted`) a été extraite de `CataloguePage.tsx` vers `src/lib/accessState.ts` pour être réutilisée telle quelle par `ProfilePage.tsx` — `CataloguePage.tsx` importe désormais la version partagée au lieu de sa fonction locale. `getProjectsByIds()` (`src/data/projects.ts`) ajoutée pour ne récupérer que les projets effectivement liés à une expérience (`projects_catalog_view`, `select ... in (ids)`) plutôt que tout le catalogue sur `profileLoader` — préserve l'optimisation du 22/07 (`getProjects()` volontairement absent du chemin critique de la page profil).
- **CV PDF — comportement changé le 08/09** : `uploadDesignerCv()` (`src/lib/storage.ts`) stocke désormais l'URL publique **nue** (`getPublicUrl(path)`, sans `{ download: true }`) — le bouton "Afficher le CV en pdf" (`ProfilePage.tsx`, renommé une 2e fois le 08/09 depuis "Voir le CV en détail") ouvre le PDF inline dans un nouvel onglet (`target="_blank"`) plutôt que de forcer un téléchargement immédiat ; le téléchargement reste possible depuis la visionneuse PDF du navigateur. `deleteDesignerCv()` tolère toujours un `?download` hérité d'une URL enregistrée avant ce changement mais n'en génère plus. Reste inchangé : bucket `designer-cv`, `cv_url` sur `designer_profiles`, `updateDesignerProfile()` implémente le pattern **« capturer avant / supprimer après »** (lecture fraîche de l'ancien `cv_url` juste avant l'`UPDATE`, nettoyage Storage best-effort de l'ancien fichier après coup si l'URL a changé) — premier usage de ce pattern dans le repo, à réutiliser pour tout futur champ fichier remplaçable (`thumbnail_url`/`photo_url` ne le font pas aujourd'hui, cf. piège documenté plus haut sur l'absence de nettoyage au remplacement). Détail complet (numérotation calculée, couleurs, anatomie accordéon, CRUD) dans `DESIGN.md`.
- **Accordéon, 2e retouche le 08/09 (même jour)** : marges de la card Expériences réalignées sur celles de la card Hero (`md:p-12`) ; `short_desc` affichée sur plusieurs lignes (plus de `truncate`), chevron recentré verticalement sur toute la hauteur de l'en-tête ; coins carrés sur les items (`rounded-xl` retiré, demande explicite) ; première mission dépliée par défaut ; boutons projet unifiés en style **secondary** (`bg-secondary-container`), "Voir le projet" renommé "Détails du projet" ; "Voir plus d'expériences" sorti de la liste `divide-y`, devenu un bouton primary autonome centré. Les champs "Description courte — section Parcours" (renommé, cf. ci-dessous) et "CV (PDF)", jusque-là dans le formulaire général "Mes Paramètres" (`ParametresTab`), sont désormais rendus dans `ExperiencesManager` ("Mon Parcours") — état (`form.cvUrl`/`form.experiencesIntro`, `pendingCvFile`, `cvError`) et sauvegarde (`handleSave`, toggle "Modifier mes informations") restent dans `ParametresTab`, simplement descendus en props à `ExperiencesManager` : ces deux champs continuent de s'enregistrer via le bouton "Enregistrer" du formulaire principal, pas via un save indépendant.
- **Accordéon, 3e retouche le 09/09** : bouton "Détails du projet" repassé en **outline au style des boutons icône du Hero** (`border-white/15`/`text-primary`, plus aucune couleur `secondary`) — "Accéder au projet confidentiel" reste filled `secondary`, les deux boutons désormais **centrés horizontalement** dans le contenu déplié (`<div className="flex justify-center">` posé dans `ProjectAction`, pas sur tout le pied de panneau pour ne pas centrer aussi les `Alert` `pending`/`refused`). Marge entre la colonne "Parcours" et l'accordéon d'abord réduite (`gap-10` → `gap-6` sous `lg`), puis agrandie dans l'autre sens sur retour immédiat (`gap-6` → `gap-10` empilé, `lg:gap-12` → `lg:gap-20` côte à côte), puis ré-agrandie une 3e fois en desktop uniquement (`lg:gap-20` → `lg:gap-28`), puis supprimée une 4e fois côté empilé mobile/tablette uniquement (`gap-10` → `gap-0`, `lg:gap-28` desktop inchangé).
- **Contenu des expériences + CV réel (09/09)** : `bullets`/`impact`/`short_desc` des 7 expériences relus et corrigés à partir du CV `~/Downloads/CVEnoraLeTurnier-ProductDesignerNocode.pdf` (mise à jour de contenu par SQL direct, pas de migration — quelques imprécisions corrigées : détail Sercel "exploration des sous-sols", CDC "(Adobe XD)"/"en lien avec le Design", dates individuelles de chaque ligne "Expériences complémentaires" restaurées, notamment Apside 2019–2024 qui manquait). Le vrai fichier CV a été uploadé dans le bucket `designer-cv` (via un script Node ponctuel utilisant `SUPABASE_SERVICE_ROLE_KEY`, supprimé après usage, pas conservé dans `scripts/`) et `designer_profiles.cv_url` mis à jour en conséquence — l'ancien fichier de test (`1788881706069.pdf`, uploadé le 08/09 pour vérifier le flux admin) a été supprimé du bucket.
- **Accordéon, 5e retouche le 09/09** : plus aucun item déplié par défaut (`openId` revenu à `null`, sur retour explicite après "première mission dépliée par défaut" du 08/09).
- **Accordéon, 4e retouche le 09/09** : dates de la colonne fixe desktop (`hidden md:block`) passées sur deux lignes (`formatExperiencePeriodParts`, deux `<span className="block">`, pas de tiret) — le format une-ligne avec tiret (`formatExperiencePeriod`) reste utilisé pour la réplique mobile inline (`md:hidden`), la demande visait explicitement le desktop. Boutons "Afficher le CV en pdf"/"Détails du projet"/"Accéder au projet confidentiel"/"Voir plus d'expériences" passés en `max-md:w-full` (pleine largeur mobile, cohérent avec "Voir les projets" du Hero) ; "Détails du projet" ramené à `max-md:w-[70%]` juste après sur retour immédiat (reste centré via le wrapper `flex justify-center`). Section élargie de 48px **à droite uniquement** (`lg:-mr-12` sur la `<section>`) — le numéro "02" reste aligné avec "01"/"03" (marge négative jamais posée à gauche). Titre public "Expériences" renommé **"Parcours"** (`<h2>` de `ProfilePage.tsx` + libellé/placeholder admin "Description courte — section Parcours") pour matcher la terminologie déjà utilisée côté admin ("Mon Parcours") — identifiants internes inchangés (`id="experiences"`, `SECTION_NUMBER_CLASSES.experiences`, table `experiences`, `ExperiencesManager`, bouton "Voir plus d'expériences").
- **Webhooks Resend** (`dispatch_webhook()` générique + 4 triggers) : domaine non vérifié → tous les emails partent de `onboarding@resend.dev` (mode test). `normalizeGmail()` strip le `+alias` avant envoi (Resend fait une comparaison stricte, ignore les alias Gmail) — uniquement pour gmail.com/googlemail.com. **Faux positif déjà rencontré (14/07)** : après un insert `contacts`, la notification admin peut mettre plusieurs minutes à arriver (latence de livraison depuis `onboarding@resend.dev`, domaine partagé) — vérifié via `net._http_response` (200 + id Resend valide immédiatement) puis confirmé reçu en boîte après coup. Ne pas conclure trop vite à un bug si l'email n'est pas encore là juste après le test, re-checker (spam inclus) après quelques minutes.
- **⚠️ Mode test Resend = destinataire unique, pas juste expéditeur partagé (19/07)** : sans domaine vérifié (`list-domains` API Resend → vide), Resend **rejette en 403** (`validation_error`, avant toute mise en file) tout envoi vers une adresse autre que celle du compte Resend lui-même (`enoraleturnier@gmail.com`) — confirmé via `net._http_response` (`status_code: 502`, body `{"sent":false,"result":{"status":403,...}}` côté `webhook-welcome-email`, ou `results.visitor.status: 403` côté `webhook-access-request-created`) et `list-emails` (aucun envoi historique vers une adresse hors `enoraleturnier@gmail.com`, tous `delivered`/`opened`). Les alias `enoraleturnier+xxx@gmail.com` passent uniquement parce que `normalizeGmail()` les réduit à `enoraleturnier@gmail.com` avant l'appel Resend — un vrai visiteur externe (adresse Gmail non-alias, ou tout autre domaine) ne recevra **jamais** aucun email tant qu'un domaine n'est pas vérifié sur resend.com/domains (et `from:` mis à jour dans les 4 Edge Functions `webhook-*`). Le code applicatif (`dispatch_webhook`, secret partagé, triggers) fonctionne correctement — ce n'est pas un bug à chercher côté code.
- **Sécurité webhooks (15/07)** : `verify_jwt: true` sur une Edge Function n'empêche PAS un appel direct par n'importe qui — ça vérifie juste "une clé Supabase valide", et la clé publique (`sb_publishable_...`) en est une, donc accessible à tout le monde par construction. Les 4 fonctions `webhook-*` (censées n'être déclenchées que par `dispatch_webhook()`) vérifient désormais un header `x-webhook-secret` en plus, comparé à un secret stocké en **Vault** (jamais en dur dans une migration) via `get_webhook_dispatch_secret()` (`SECURITY DEFINER`, `EXECUTE` réservé à `service_role`) — même pattern que le `x-cron-secret` déjà en place sur `sync-notion-veille`. Avant ce fix, `webhook-welcome-email`/`webhook-contact-created` étaient un relais mail ouvert (destinataire + contenu 100% contrôlés par l'appelant, zéro vérification en base). Les 4 Edge Functions `probe-*` (`probe-resend-key`/`probe-resend-send`/`probe-resend-status`/`probe-anthropic-key`, jamais versionnées localement, créées pendant le diagnostic de délivrabilité et jamais nettoyées) ont été neutralisées (stub 410) pour la même raison — `probe-resend-send` en particulier était un relais mail ouvert encore plus direct (`{"to": "..."}` sans aucune validation). Suppression complète possible via le dashboard Supabase si besoin (pas d'outil MCP pour ça).
- **IA de structuration** (`generate-ai-description`) : utilise Mistral (`mistral-small-latest`), pas Anthropic (migré pour coût). Limites de caractères par champ dupliquées manuellement entre l'Edge Function (`FIELD_MAX_LENGTH`) et `src/lib/projectValidation.ts` (`MAX_LENGTHS`) — aucun import partageable Vite/Deno, garder synchronisé à la main.
- **Formulaire de contact / "Messages reçus"** : `ContactForm.tsx` (profil public) et l'onglet "Messages reçus" du dashboard admin branchés au réel (14/07) via `src/data/contacts.ts` (`submitContact`/`getAllContacts`/`updateContactStatus`) — RLS déjà en place (`contacts_insert_anyone`/`contacts_select_admin`/`contacts_update_admin`), aucune policy créée pour cette passe. Champ "Entreprise" retiré (pas de colonne correspondante). Statut à sens unique (`new → treated → archived`) : un message archivé ne redevient jamais `new`, cf. `nextContactStatus` dans `AdminPage.tsx`. `getAllContacts()` joint aussi `user_profiles.company` (14/07, pour la liste "Mes contacts" du Dashboard) quand `contacts.user_id` est renseigné.
- **Notifications (14/07, vue mobile le 17/07)** : table `notifications` + trigger `access_requests_notify` (`SECURITY DEFINER`, migration `20260714160000_create_notifications.sql`) — une nouvelle `access_requests` en `pending` notifie tous les admins (`access_request_received`), son passage `pending → approved/rejected` notifie le visiteur (`access_request_resolved`). RLS : chacun ne voit/modifie que ses propres notifications (`user_id = auth.uid()`). Desktop : `NotificationBell` (monté une fois dans `Header.tsx`, déjà commun admin/visiteur) — même cloche des deux côtés, contenu différent uniquement via RLS. Mobile : `MobileNotificationsView` (drill-in dans `MobileAccountSheet`, même table, même RLS, `notificationLabel()` partagé entre les deux pour un seul format de libellé) — aucune table/migration dédiée à la vue mobile. Clic sur `access_request_received` → `/admin?tab=demandes` ; clic sur `access_request_resolved` → catalogue avec le projet concerné remonté en tête de liste via `?notif=<project_id>` (tri client-side dans `CataloguePage.tsx`, pas de scroll).

## Comptes de test

- **Toujours de vraies adresses Gmail + alias** (`enoraleturnier+xxx@gmail.com`), jamais un domaine inventé — Resend doit pouvoir délivrer réellement. `PersonaSwitcher.tsx` a les emails en dur (`PERSONAS`, dont un compte `admin`) : à resynchroniser si les comptes changent.
- **`PersonaSwitcher` restreint au dev local uniquement depuis le 04/09** (visible en dev local ET sur les previews Vercel entre le 15/07 et le 04/09, jamais sur le vrai domaine de prod) — gating dans `RootLayout.tsx` : `import.meta.env.DEV` seul désormais (l'ancienne branche `import.meta.env.VITE_VERCEL_ENV === "preview"` a été retirée). Revert décidé après avoir découvert que `PERSONAS` (email + mot de passe en dur, dont un compte `admin` réel sur la prod) était donc committé dans un repo GitHub **public** et finissait dans le bundle JS de chaque preview Vercel, sans que la *Deployment Protection* (Project Settings → Deployment Protection, jamais vérifiée comme active) ne soit confirmée comme rempart réel. `import.meta.env.DEV` est `false` pour tout build Vite (`vite build`), preview ou prod — confirmé en buildant réellement les deux (`vite build --mode production` avec/sans `VERCEL_ENV=preview`) et en grepant le `dist/` : `PersonaSwitcher` (et son contenu) n'apparaît plus dans aucun des deux. Le mot de passe du persona admin est en plus sorti du code source vers `VITE_PERSONA_ADMIN_PASSWORD` (`.env`, gitignored, jamais répliqué côté Vercel puisque plus nécessaire) — `import.meta.env.VITE_PERSONA_ADMIN_PASSWORD ?? ""` dans `PersonaSwitcher.tsx`, absent = connexion échoue proprement (erreur affichée, pas de crash). Rotation effectuée le 04/09 (ancien mot de passe testé en direct : rejeté). Personas `Sophie`/`Karim` (rôles non-admin) gardent un mot de passe en dur (`Test1234!`) — accepté car ce fichier ne finit plus dans aucun bundle déployé, seulement en dev local.
- Scripts admin réutilisables dans `scripts/` (lisent `SUPABASE_SERVICE_ROLE_KEY` depuis `.env.admin`, gitignored).

## Déploiement

- **Vercel** : projet `folio` (team `enora-le-turnier-s-projects`), connecté au repo GitHub, déploie automatiquement chaque push sur `main` en prod et chaque branche/PR en preview. Pas de domaine personnalisé pour l'instant, uniquement les sous-domaines `*.vercel.app` générés par Vercel (HTTPS automatique, identique à un domaine personnalisé sur ce plan). Variables d'env requises au build : `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY` (jamais `SUPABASE_SERVICE_ROLE_KEY`, qui n'a aucun usage côté Vercel — cf. Base de données ci-dessous et `scripts/`).

## Architecture

- **Routing** : React Router SPA pure (`createBrowserRouter`), plus de TanStack Start/SSR. `src/router.tsx` (routes+loaders), `src/main.tsx` (entry CSR), `index.html` statique avec script `THEME_INIT` inline (seul responsable de la classe `dark`/`light` sur `<html>`, avant tout rendu React).
- **`/` sert directement le profil, plus de redirect (22/07)** : `router.tsx` faisait `loader: () => redirect(...)` vers `/enora-le-turnier` sur `path: "/"` — un aller-retour réseau complet avant même de démarrer le chargement du vrai profil. `/` et `/:slug` pointent désormais tous les deux vers `ProfilePage`/`profileLoader` (même élément, même loader, définis une seule fois sous `SlugLayout`). `profileLoader` accepte maintenant `params.slug` absent (route `/`) en plus de `params.slug === designer.slug` (route `/:slug`) — seul un slug présent mais différent reste une vraie 404. Les liens internes vers `/enora-le-turnier` (Header, Footer, etc.) restent inchangés et fonctionnent toujours, `/` est un raccourci en plus, pas un remplacement.
- **Code-splitting par route (22/07)** : toutes les routes sauf `ProfilePage` (page d'accueil `/:slug`, gardée en import statique — c'est la première page qu'un visiteur charge, pas d'aller-retour réseau supplémentaire à lui ajouter) passent par la prop `lazy` de React Router 7 (`route.lazy: async () => ({ Component, loader })`, pas `React.lazy`+`Suspense` manuel) — chaque page devient son propre chunk Vite chargé à la demande. Avant ça, `router.tsx` important tout statiquement, un visiteur du profil public téléchargeait aussi `AdminPage.tsx` (~2600 lignes) + `react-markdown`/`remark-gfm`/`remark-breaks` (utilisés uniquement par `MarkdownContent`, pages légales/aide) + `date-fns`+locale (Veille Hebdo) dans un unique bundle de 1 002 Ko (281 Ko gzip) — Vite avertissait déjà du seuil 500 Ko dépassé. Exception : `NotFoundPage` reste en import statique dans `router.tsx` (route `*`) malgré le refactor — `RouteError.tsx` l'importe déjà statiquement pour l'`errorElement` racine, un second import dynamique n'aurait donné aucun gain (Vite ne peut pas sortir un module dans son propre chunk s'il est aussi importé statiquement ailleurs, warning `INEFFECTIVE_DYNAMIC_IMPORT` sinon).
- **`@tanstack/react-query` retiré (22/07)** : `RootLayout.tsx` créait un `QueryClient` et enveloppait toute l'app dans `QueryClientProvider`, mais aucun composant n'appelait `useQuery`/`useMutation` nulle part dans le code (toutes les données passent par les loaders React Router, cf. ci-dessus) — poids mort pur au bundle et à l'exécution. Dépendance retirée du `package.json`.
- **`profileLoader` n'appelle plus `getProjects()` (22/07)** : la page profil n'affiche aucune carte projet, seulement un booléen (`hasConfidential`, pour le bouton "Accéder aux projets confidentiels") — `getProjects()` (`select *` sur toute `projects_catalog_view`) était donc un fetch bloquant totalement inutile sur le chemin critique de la page d'accueil. Remplacé par `hasConfidentialProject()` (`src/data/projects.ts`), un `select("id", { count: "exact", head: true }).eq("status", "confidential")` qui ne rapatrie aucune ligne, juste un count via header HTTP.
- **Storage** : bucket `project-thumbnails` (public, écriture admin only) pour les thumbnails projets ; bucket `designer-photos` (même pattern RLS) pour la photo de profil ; bucket `designer-cv` (07/09, même pattern RLS, 4 policies posées dès le départ) pour le CV PDF téléchargeable du profil public.
- **Versioning** : SemVer dès `main` — `fix:` → PATCH, `feat:` → MINOR (reste en `0.x`, `1.0.0` sera délibéré). Toute fusion sur `main` avec changement utilisateur significatif doit être suivie d'un tag + release GitHub.
- **Navigation mobile (17/07, `< md`/768px)** : `Header.tsx` porte désormais un bloc mobile complet (burger + thème + compte), commun pages publiques et dashboard admin — détail complet (composants, breakpoints, convention "mobile-only") dans `DESIGN.md` section "Navigation mobile". `AdminSidebar` passée en `hidden md:flex` (remplacée par `AdminMobileBottomNav` sur mobile, Paramètres déplacé dans la feuille compte sous "Préférences") — ne jamais réintroduire l'ancienne sidebar icône-seule en mobile. `useThemeMode` (`src/hooks/useThemeMode.ts`) est la seule source de vérité pour le thème, partagée entre `ThemeToggle` (desktop) et `MobileThemeSheet` — ne pas dupliquer l'état thème ailleurs.
- **Overlays et filtres mobile (17/07)** : tiroirs latéraux (`ProjectDrawer`, asides en lecture seule d'`AdminPage`) et footers de boutons (`ProjectDrawer`, `AccessRequestModal`, confirmations `AdminPage`) responsive sous `md` — détail (classes exactes, avant/après) dans `DESIGN.md` section "Overlays et filtres mobile". `FilterBar`/`AdminFilterBar` : pills en scroll horizontal (`.scrollbar-hide`, `styles.css`) + panneau "Filtrer" en `SlideSheet from="left"` sous `md` au lieu de l'expand inline desktop — `SlideSheet` a gagné une prop `className` pour ce pattern (variante mobile-only rendue en parallèle d'une variante desktop, gating CSS `md:hidden`/`hidden md:flex`, pas de media query JS).
